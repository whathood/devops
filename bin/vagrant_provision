#!/usr/bin/env ruby

cmds = [
  "vagrant halt",
  "rm -rf whathood/log",
  "rm -rf whathood/scr",
  "mkdir -p whathood/log && mkdir -p whathood/src",
  "time ./bin/vagrant_up --provision"
]

cmds.each { |cmd|
  puts cmd
  unless system(cmd)
    abort "could not execute command: '#{cmd}'"
  end
}
