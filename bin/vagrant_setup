#!/usr/bin/env ruby

# run vagrant for the first time

cmds = [
  "vagrant plugin install vagrant-vbguest || true",
  "vagrant plugin install vagrant-triggers || true",
  "vagrant plugin install vagrant-rsync-back || true",
  "vagrant up --provision"
]

cmds.each { |cmd|
  puts cmd
  unless system(cmd)
    abort "could not execute command: '#{cmd}'"
  end
}
